<Window x:Class="DesktopWPF.Views.AttendanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AttendanceView" Height="300" Width="400">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Event.Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
            <ListBox ItemsSource="{Binding Event.Attendances}" SelectionMode="Multiple">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Customer.Name}" Margin="0,0,10,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Save Attendance" Click="SaveAttendanceButton_Click" HorizontalAlignment="Center" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>